{{systemStandard}}

Create the implementation plan for this Next.js project.

Next.js selected blueprint:
{{adapter.promptContext.environment_blueprint}}

Next.js-specific planning guidance:
- Account for App Router vs Pages Router, Server Components vs Client Components, route handlers, Server Actions, package scripts, and the existing application state.
- If the selected database runtime is not `none`, plan around `DATABASE_URL` in `.env.local` and keep database access in server-only code.
- Include the focused verification path needed for the selected stack.
