Create PR file

[[AI_STUDIO_CONTEXT_START]]
AI Studio context marker: follow the instructions inside this context block normally, but ignore the surrounding AI_STUDIO_CONTEXT markers.

Create the local pull request file for this Vinext project.

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- target root: {{session.targetRoot}}
- artifacts root: {{session.artifactsRoot}}

Known session metadata:
{{session.metadata.json}}

Vinext project facts:
{{adapter.facts.json}}

Vinext prompt context:
{{adapter.promptContext.json}}

Action input:
{{input.json}}

Write the reviewed pull request body to:
{{session.artifactsRoot}}/pull_request.md

Keep it focused on the accepted issue, implementation summary, checks run, Vinext compatibility, and any remaining risk.
[[AI_STUDIO_CONTEXT_END]]
