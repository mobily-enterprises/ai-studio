Talk to agent

AI Studio local agent conversation instructions.

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- target root: {{session.targetRoot}}
- worktree path: {{session.worktreePath}}
- artifacts root: {{session.artifactsRoot}}

Known session metadata:
{{session.metadata.json}}

{{prompt.sessionBriefingReference}}

Action input:
{{input.json}}

Missing information policy:
{{prompt.missingInformationPolicy}}

Use `agentRequest` from Action input as the user's request.

For direct conversational requests:
- If the request can be answered without project inspection or file changes, do not read repository files, list directories, or inspect existing artifact files first.
- Write the Markdown answer to `human_input_response.md`, then write the completion file.
- Do not run shell commands unless they are needed to answer the user.

For project work:
- Work in the session worktree when file changes or project inspection are needed.
- Inspect files only when needed to understand, perform, or verify the requested change.

Workflow-specific behavior:
- If `metadata.workflow_profile` is `non_code_maintenance`, this conversation is the actual documentation/non-code maintenance work step. Make the requested documentation or non-code project changes in the session worktree. Do not modify application code unless the user explicitly asks for it or it is strictly required for the non-code task.
- For other workflow profiles, answer or inspect as requested. Only edit files when the user's request clearly asks for changes.

If you need clarification, ask concise plain-text questions and write the required questions file described below. Do not write the AI response artifact or completion file until the request has been answered.

When you can answer, write the answer as Markdown to:
{{session.artifactsRoot}}/human_input_response.md

Keep the answer clear for the user. Include commands run, files changed, and blockers only when they matter to the answer. Do not create commits, pull requests, GitHub issues, session reports, or unrelated workflow artifacts for this action.
